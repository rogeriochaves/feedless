<%- include('_header') %>

<form action="publish" method="POST">
  <textarea name="message"></textarea>
  <input type="submit" value="Send" />
</form>
<p>
  Whoami: <%= profile.id %>
</p>
<% posts.map(post => { %>
  <div><%= post.text %></div>
<% }) %>

<%- include('_footer') %>